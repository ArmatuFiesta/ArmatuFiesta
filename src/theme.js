import { createMuiTheme } from '@material-ui/core/styles';


const theme = createMuiTheme({
  palette: {
    primary: {
        main: "#F8BBD0",
      },
      secondary: {
        main: "#3DB9F6",
      },
      leters: {
        main: "#FFFFFF",
        sub:"#000000",
      },
  },
});

export default theme;