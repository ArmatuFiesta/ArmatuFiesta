import { createMuiTheme } from '@material-ui/core/styles';


const theme = createMuiTheme({
  palette: {
    primary: {
        main: "#d1f175",
      },
      secondary: {
        main: "#d45b00",
      },
      leters: {
        main: "#FFFFFF",
        sub:"#000000",
      },
  },
});

export default theme;